#!/bin/bash
wp plugin install theme-check --activate --allow-root # Theme Check
wp plugin install query-monitor --activate --allow-root # Query Monitor
wp plugin install debug-bar --activate --allow-root # Debug Bar
wp plugin install debug-bar-console --activate --allow-root # Debug Bar Console
wp plugin install debug-bar-cron --activate --allow-root # Debug Bar Cron
wp plugin install debug-bar-list-dependencies --activate --allow-root # Debug Bar List Dependencies
wp plugin install debug-bar-post-types --activate --allow-root # Debug Bar Post Types
wp plugin install debug-bar-remote-requests --activate --allow-root # Debug Bar Remote Requests
wp plugin install debug-bar-slow-actions --activate --allow-root # Debug Bar Slow Actions
wp plugin install debug-bar-super-globals --activate --allow-root # Debug Bar Super Globals
wp plugin install debug-bar-taxonomies --activate --allow-root # Debug Bar Taxonomies
wp plugin install debug-bar-constants --activate --allow-root # Debug Bar Constants
wp plugin install debug-bar-actions-and-filters-addon --activate --allow-root # Debug Bar Actions and Filters Addon
wp plugin install debug-bar-shortcodes --activate --allow-root # Debug Bar Shortcodes
wp plugin install debug-bar-rewrite-rules --activate --allow-root # Debug Bar Rewrite Rules